@using NetBook.Common
@model NetBook.Web.Paging.PaginatedList<NetBook.Web.ViewModels.Remark.RemarkAllViewModel>
@{
    this.ViewData["Title"] = $"Забележки";
}

<main id="main">
    <div class="container">
        <div class="row">
            <div class="col s12">
                <div class="title" style="position: relative">
                    <div class="row" id="white">
                        <div class="col s12 m12 l9 xl8" style="margin-top: 28px">
                            <h4 class="white-text">
                                Забележки
                            </h4>
                        </div>
                    </div>
                </div>
                <div class="card-panel white" style="overflow-x: auto;">
                    <table class="highlight">
                        <thead>
                            <tr>
                                <th>Текст</th>
                                <th>Ученик</th>
                                <th>Предмет</th>
                                <th>Действия</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var remark in @Model)
                            {
                                <tr style="cursor: default;">
                                    <td>@remark.Text</td>
                                    <td>@remark.StudentFullName</td>
                                    <td>@remark.SubjectName</td>

                                    <td>
                                        <a asp-area="Administration" asp-controller="Remark" asp-action="Delete" asp-route-id="@remark.Id" class="indigo-text text-lighten-3 tooltipped" data-position="bottom" data-delay="50" data-tooltip="Изтриване">
                                            <i class="material-icons red-text ">delete</i>
                                        </a>
                                    </td>

                                </tr>
                            }
                        </tbody>
                    </table>
                    @{
                        var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
                        var nextDisabled = !Model.HasNextPage ? "disabled" : "";
                    }
                    <div class="center-align">
                        <a asp-area="Administration" asp-controller="Remark" asp-action="All"
                           asp-route-pageNumber="@(Model.PageIndex - 1)"
                           class="btn waves-effect waves-light gradient-animation  @prevDisabled">
                            Предишна
                        </a>
                        <a asp-area="Administration" asp-controller="Remark" asp-action="All"
                           asp-route-pageNumber="@(Model.PageIndex + 1)"
                           class="btn waves-effect waves-light gradient-animation @nextDisabled">
                            Следваща
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>
